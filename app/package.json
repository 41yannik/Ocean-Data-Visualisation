{
  "name": "track-to-toll",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test ../tests/*.test.mjs",
    "test:browser": "cd .. && if [ -x \"${CONDA_PYTHON_EXE:-}\" ]; then \"$CONDA_PYTHON_EXE\" tests/browser_audit.py; else python3 tests/browser_audit.py; fi",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@fontsource-variable/bricolage-grotesque": "^5.2.10",
    "d3": "^7.9.0",
    "scrollama": "^3.2.0",
    "topojson-client": "^3.1.0"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "world-atlas": "^2.0.2"
  }
}
