# System
.DS_Store

# Dependencies & Build
node_modules/
dist/

# Rohdaten — nicht im Repo (Größe + Lizenzen; EM-DAT: internal use only)
# ohne Slash: matcht auch den Data-Symlink in der Arbeitskopie
/Data
/Developer API/

# EM-DAT-Derivate der Kurs-Variante — Lizenz erlaubt keine Weiterverbreitung
# (Challenge-Variante ohne EM-DAT wird separat freigegeben, siehe docs/decisions/)
app/public/data/events*.json
app/public/data/meta*.json
