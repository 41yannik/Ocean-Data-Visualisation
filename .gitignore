# System
.DS_Store

# Dependencies & Build
node_modules/
dist/

# Rohdaten — nicht im Repo (Größe + Lizenzen; EM-DAT: internal use only)
# ohne Slash: matcht auch den Data-Symlink in der Arbeitskopie
/Data
/Developer API/

# Hinweis: events*.json / meta*.json enthalten EM-DAT-Derivate. Bewusste Entscheidung
# (2026-07): für das öffentliche GitHub-Pages-Deployment freigegeben (Lizenz vom Betreiber
# abgeklärt). Die Roh-Quelle /Data (u. a. der große EM-DAT-Export) bleibt unten ignoriert.

# Python
__pycache__/
*.pyc

# Playwright/Verifikations-Screenshots (lokale Prüfung, nicht ins Repo)
.playwright-mcp/
